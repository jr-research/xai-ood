[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xai-ood"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy==1.26.4",
  "pandas",
]

[project.optional-dependencies]
test = ["pytest", "scipy", "scikit-learn"]

[tool.setuptools.packages.find]
where = ["src"]

